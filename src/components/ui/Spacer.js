import "../../styles/components/ui/Spacer.css";

// Spacers will automatically adjust their width or height based on the remaining space in their parent container.
const Spacer = () => {
  return <div className="spacer" />;
};

export default Spacer;
